import { Container, Typography } from '@mui/material';

const Orders = () => {
  return (
    <Container maxWidth="lg" sx={{ py: 4 }}>
      <Typography variant="h4" gutterBottom>
        My Orders
      </Typography>
      <Typography>Orders page - To be implemented</Typography>
    </Container>
  );
};

export default Orders;

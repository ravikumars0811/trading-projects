import { Container, Typography } from '@mui/material';

const Wishlist = () => {
  return (
    <Container maxWidth="lg" sx={{ py: 4 }}>
      <Typography variant="h4" gutterBottom>
        Wishlist
      </Typography>
      <Typography>Wishlist page - To be implemented</Typography>
    </Container>
  );
};

export default Wishlist;

import { Container, Typography } from '@mui/material';

const Cart = () => {
  return (
    <Container maxWidth="lg" sx={{ py: 4 }}>
      <Typography variant="h4" gutterBottom>
        Shopping Cart
      </Typography>
      <Typography>Cart page - To be implemented</Typography>
    </Container>
  );
};

export default Cart;

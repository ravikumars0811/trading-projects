import { Container, Typography } from '@mui/material';

const ProductDetail = () => {
  return (
    <Container maxWidth="lg" sx={{ py: 4 }}>
      <Typography variant="h4" gutterBottom>
        Product Detail
      </Typography>
      <Typography>Product detail page - To be implemented</Typography>
    </Container>
  );
};

export default ProductDetail;

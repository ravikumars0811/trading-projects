import { Container, Typography } from '@mui/material';

const Checkout = () => {
  return (
    <Container maxWidth="lg" sx={{ py: 4 }}>
      <Typography variant="h4" gutterBottom>
        Checkout
      </Typography>
      <Typography>Checkout page - To be implemented</Typography>
    </Container>
  );
};

export default Checkout;

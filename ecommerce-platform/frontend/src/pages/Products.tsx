import { Container, Typography } from '@mui/material';

const Products = () => {
  return (
    <Container maxWidth="lg" sx={{ py: 4 }}>
      <Typography variant="h4" gutterBottom>
        Products
      </Typography>
      <Typography>Product listing page - To be implemented</Typography>
    </Container>
  );
};

export default Products;
